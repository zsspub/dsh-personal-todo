# 个人待办计数更新修复

关联待办：b49ba296-764e-4a28-93c0-9fc96c9b9d45
日期：2026-09-10
状态：代码与自动检查完成，真实浏览器验收及 GIF 受阻。

## 改动

- 面板打开时沿用 2 秒间隔刷新，不再依赖当前列表是否含执行中待办。
- 侧栏与面板统一计入待开始、待补充、待审核。
- 更新统计范围测试，增加面板打开/关闭时外部新增待办的回归测试，并验证关闭面板及卸载后的轮询清理。
- 更新客户端构建产物；保留工作区既有改动，未提交或推送。

## 自动检查

- 修复前：新增/更新的 3 项回归用例均失败。
- 修复后：5 个测试文件、71 项测试通过；语句覆盖率 97.24%。详见 tests.log。
- Host、Client、测试类型检查通过；全仓库 ESLint 与 git diff --check 通过。
- 客户端构建通过（tsc、tsdown、normalize-artifacts）。
- pnpm run check 持续无输出，已终止；改为运行 node_modules/.bin 中的本地工具。没有宣称完整 check 或打包检查通过。
- 测试有依赖包缺少 source map 和 Vitest environmentMatchGlobs 弃用提示，未导致失败。

## 真实 E2E 与 GIF 阻塞

agent-browser 无法写入沙箱外 socket 目录；浏览器连接清单可见真实 DSH，但访问 http://127.0.0.1:3080 被浏览器安全策略拒绝，工具明确指出用户拒绝访问且禁止绕行。已停止浏览器操作。

本次未完成真实 E2E、未生成录屏或 GIF，未复用历史证据。仍待验证：Agent 实际新增待办后，关闭面板时侧栏更新、打开空面板时列表及导航计数自动更新、关闭重开与页面刷新后结果保持一致。
